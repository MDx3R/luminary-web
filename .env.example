# API base URL (backend; use URL reachable from the browser)
# e.g. http://localhost:8000 in dev, http://app:8000 when frontend runs in Docker and calls backend
NEXT_PUBLIC_API_URL=http://localhost:8000
