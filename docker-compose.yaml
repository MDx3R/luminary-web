services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-3000}:3000"
    environment:
      NODE_ENV: production
    networks:
      - luminary

networks:
  luminary:
    driver: bridge
    external: true
